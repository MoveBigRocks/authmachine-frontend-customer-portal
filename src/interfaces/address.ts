export interface AddressInterface {
    id: string,
    formatted: string,
    country: "UA",
    locality: string,
    postalCode: string,
    primary: boolean,
    region: string,
    streetAddress: string,
    type: string,
}